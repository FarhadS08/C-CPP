/*
CH-230-A
a1 p2 [c]
Farhad Suleymanzade
fsuleymanz@jacobs-university.de
*/

#include <stdio.h>

int main() {
	int result; // The result of of calculation
	result = (2+7) * 9/3; 
	printf("The result is %d\n", result);
       	return 0;
}

//  Because %d expects an argument, we have to assign result as an argument
